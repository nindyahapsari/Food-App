export const baseUrl = 'http://localhost:3001/'

// In terminal you have to map the port to connect the local server. 
// the port from the computer is different from the emulator that is why the network request failed. 
// to change that is to reverse it 
// type in terminal "adb reverse tcp:'number of the local port computer' tcp:'the same portnumber in computer for the emulator or real device' "